services:
  # Option 1: Static Site Deployment
  - type: static
    name: clip-n-copy-static
    buildCommand: npm run build
    staticPublishPath: .output/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  # Option 2: Web Service Deployment (Node.js SSR Server)
  # - type: web
  #   name: clip-n-copy-web
  #   env: node
  #   buildCommand: npm run build
  #   startCommand: node .output/server/index.mjs
